---
import Base from "../layouts/Base.astro";
import { SITE_NAME } from "../consts";
import SecuritySections from "../components/sections/SecuritySections.astro";
---

<Base
	title={`Security | ${SITE_NAME}`}
	description="ReaderPub reduces large-scale ebook leakage risk by keeping primary reading access on the web instead of distributing a standard downloadable book file, while giving full control over access and distribution."
>
	<SecuritySections standalone />
</Base>
